---
name: 知学空间
description: 面向个人学习者的 Obsidian 式教材知识库与复习工作台
---

# Design System: 知学空间

## Creative North Star

**“个人学习库房”**

界面借用 Obsidian 的工作语法，而不是复制其品牌外观：知识库位于左侧，学习内容在中央标签工作区展开，AI 家教位于右侧并跟随当前教材、题目或关联图上下文。整体像一个可长期积累、随时继续的个人学习环境，而不是课程管理后台或指标卡片墙。

设计系统的实现来源是 `apps/web/src/components/workspace/workspace-shell.module.css`、`apps/web/src/components/workspace/use-workspace-breakpoint.ts` 及对应工作区组件。本文记录已落地的真实 token 和交互规则，不定义尚未实现的主题或组件。

## Design Principles

1. **稳定三栏语法。** 左侧负责知识库选择，中央负责任务与标签，右侧负责上下文 Tutor；中央内容拥有最高空间优先级。
2. **紧凑但可读。** 工作区采用编辑器式密度，基础字号保持 13px，辅助文字不低于 12px。
3. **一个品牌强调色。** 柔紫用于选中、焦点和主要动作；绿色只表达成功或就绪语义，不用于装饰。
4. **扁平常驻面板。** 常驻区域依靠表面明度和 1px 边界分层；阴影只属于抽屉等真正离开文档平面的浮层。
5. **功能先于装饰。** 每个按钮必须连接真实功能，或显示明确、不可误解的禁用/空/错误状态。
6. **上下文跟随选择。** 切换知识库、题目或关联图时，中央标签和 Tutor 上下文保持一致；引用必须打开其实际来源预览。

## Color Tokens

| Token | Value | Usage |
| --- | --- | --- |
| `--bg` | `#fbfbfd` | 应用背景和最浅工作区底色 |
| `--panel` | `#f7f7fa` | 常驻侧栏和次级面板 |
| `--panel-deep` | `#ffffff` | 中央主内容、输入和高可读表面 |
| `--panel-soft` | `#f2f1f7` | 导航底色、弱选中与分隔区域 |
| `--line` | `#e2e1e8` | 面板、卡片、输入和标签的 1px 边界 |
| `--text` | `#282733` | 主正文与标题 |
| `--muted` | `#74727f` | 次级说明、元信息和占位文字 |
| `--purple` | `#6558c9` | 主操作、选中项和品牌强调 |
| `--purple-deep` | `#eeeafe` | 紫色选中态的浅表面 |
| `--green` | `#167c59` | 就绪、成功和已完成语义 |

普通界面不使用额外彩色模块装饰。错误与警告仅在对应状态中出现，并同时提供文字，不以颜色作为唯一信息。

## Typography

- 字体栈：`Inter, "Microsoft YaHei", system-ui, sans-serif`。
- 基础字号：`13px`。
- 辅助标签与元信息：`12px`。
- 面板标题：通常 `14px`；品牌标识为 `15px`。
- 标题、按钮和当前项通过字重建立层级；正文不使用营销式超大标题。
- 教材正文、公式或预览内容可以使用内容适配字体，但不能改变工作区导航语言。

## Layout Grammar

### Desktop: `>= 1280px`

完整显示左知识库、中央标签工作区和右 Tutor。工作区高度为 `100dvh`，页面本身不产生横向滚动。可调整分隔条使用 `5px` 命中区，中央区域保持 `min-width: 0`。

### Tablet: `960–1279px`

左知识库与中央内容常驻；Tutor 通过右侧抽屉打开。

### Compact: `720–959px`

中央内容占满可用宽度；知识库和 Tutor 分别通过左、右抽屉打开。抽屉关闭后保留知识库选择、图谱标签和 Tutor 会话状态。

### Mobile: `< 720px`

只保留中央单任务视图。左右抽屉宽度为 `100%`、`max-width: none`；工具栏、标签和关闭控件的最小高度为 `44px`。主要任务不能依赖页面级横向滚动。

## Spacing and Shape

实现采用紧凑的工作区间距，而不是宽松营销页间距：行与小控件常用 `5–12px`，面板与内容区常用 `14–20px`，空状态和大内容区可使用 `24–28px`。空间差异用于层级，不把每段信息单独包装成卡片。

常用圆角：

- `7px`：知识树行与紧凑列表项。
- `8px`：输入、标签、抽屉按钮和次级控件。
- `9px`：选中项、主要按钮和强调控件。
- `10–12px`：内容卡片、空状态、错误与图谱状态块。
- `16px`：桌面“继续学习”主卡片。
- `999px`：仅用于具有明确语义的状态 pill。

## Elevation

- 常驻面板：无阴影，以 `--line` 和表面色区分。
- 抽屉 backdrop：`rgb(35 31 54 / 36%)`。
- 抽屉阴影：`0 24px 64px rgb(35 31 54 / 24%)`。
- 抽屉层级：backdrop `z-index: 40`，drawer `z-index: 41`。
- Tablet/Compact 抽屉宽度：`min(88vw, 380px)`；Mobile 改为全宽。

## Component Rules

### Knowledge Library

知识库名称和关联图按钮是两个独立点击目标，并分别提供可访问名称、键盘触发和焦点反馈。当前知识库同时使用浅紫表面、边界和文字变化，不只依赖颜色。

### Workspace Tabs

学习首页、题库、知识库与每个知识库的关联图在中央标签栏中打开。相同知识库的图谱标签必须去重并聚焦已有标签。标签选中态具有形态和语义属性；关闭按钮不吞掉标签本身的键盘操作。

### Study Dashboard

“继续学习”是个人首页的主要入口，桌面主卡片圆角为 `16px`，移动端为 `12px`。界面只展示真实可继续项和真实待复习数量；没有数据时显示诚实空状态。

### Tutor

Tutor 跟随当前知识库或任务上下文。未配置服务端模型 Key 时显示“模型待配置”，输入与发送控件禁用且不发请求。引用按钮打开真实 `pagePreview` 来源；在非桌面布局中打开引用后关闭 Tutor 抽屉并把用户带到知识预览。

### Drawers

抽屉是窄屏的模态替代布局：支持按钮打开、`Escape` 关闭、backdrop 关闭、关闭按钮，并将焦点返回触发器。抽屉只改变呈现方式，不销毁内部学习状态。

### Semantic States

加载、空、错误、权限不足、未配置和成功必须是不同状态，并配有文字说明。禁用控件使用 `cursor: not-allowed` 与降低透明度，但关键原因必须由附近文案解释。

## Focus and Accessibility

- 通用焦点环：`3px solid #c8bdf5`，`outline-offset: 2px`。
- 标签焦点环：同色同宽，`outline-offset: -2px`，避免在紧凑标签栏中被裁切。
- Mobile 工具栏、标签和关闭控件最小高度 `44px`。
- 当前知识库、当前标签和当前图节点不能只靠颜色表达。
- 关系图必须保留可搜索的文本节点列表，Canvas/图形不是唯一信息载体。
- 正文与背景遵循至少 4.5:1 的可读对比目标。

## Motion

- 常规交互：`160ms ease`，用于背景、边界、颜色等状态变化。
- 图谱 viewport：`180ms ease` 的 transform 过渡。
- `prefers-reduced-motion: reduce` 下：`transition-duration: .01ms !important`，`scroll-behavior: auto`。
- 不使用装饰性循环动画、视差或会打断学习的入场动画。

## Do's and Don'ts

### Do

- 让当前知识库、中央标签和 Tutor 上下文保持一致。
- 让引用、搜索、答题、关联图和继续学习连接真实 API 与真实状态。
- 用边界、表面明度和紧凑间距建立编辑器式层级。
- 在所有断点提供键盘打开/关闭、焦点返回和无横向滚动体验。
- 对空数据、失败、缺少模型配置和权限不足提供可执行说明。

### Don't

- 不复制 Obsidian 的品牌颜色、图标或视觉皮肤。
- 不加入无功能按钮、装饰性状态胶囊、指标卡墙、渐变光晕或玻璃拟态。
- 不把上传、创建、班级管理等低频操作提升为个人首页主任务。
- 不用浏览器端保存模型密钥，也不把缺少 Key 伪装成网络错误。
- 不为了紧凑而牺牲 13px 正文、12px 辅助文字、焦点可见性或移动端 44px 触控目标。
