---
name: 知学空间
description: 面向个人学习者的 Obsidian 式教材知识库与复习工作台
---

<!-- SEED: established with the user before implementation; re-run $impeccable document once there's code to capture the actual tokens and components. -->

# Design System: 知学空间

## Overview

**Creative North Star: "个人学习库房"**

界面借用 Obsidian 的工作语法，而不是复制其品牌外观：知识库常驻左侧，内容在中央标签页展开，AI 家教作为右侧上下文工具随当前教材、任务或关联图变化。整体应像一个长期积累、随时可继续的个人学习环境，而不是课程管理后台。

视觉保持高信息密度，但通过稳定分栏、克制的层级和清晰选中态降低认知负担。第一眼必须让用户知道当前知识库、今天要继续什么，以及下一步操作在哪里。

**Key Characteristics:**

- Obsidian 式窄边栏、文件列表、编辑标签和上下文侧栏
- “继续学习”与复习队列优先，管理操作按需出现
- 中性浅色表面配单一柔紫强调色
- 扁平、紧凑、可拖动的工作区，而非卡片堆叠仪表盘
- 所有视觉按钮都对应真实功能或明确的不可用状态

## Colors

采用 Restrained 策略：中性灰白负责长时间阅读，柔紫只表达选中、焦点和主要动作；成功、警告和错误颜色仅用于状态语义。

**The One Accent Rule.** 普通界面只使用一个品牌强调色，不以多色装饰区分模块。

## Typography

使用本机工作型无衬线字体栈。正文、标签、树节点和按钮强调稳定可读；教材引用、公式和长文可以使用适合内容的衬线或数学字体，但不能改变导航语言。

**The Editor Density Rule.** 信息密度可以接近编辑器，但正文、交互标签和触控目标不能因追求紧凑而低于可读标准。

## Layout

桌面工作区由三部分组成：最左知识库栏、中央标签工作区、右侧 AI 家教。知识库栏是第一导航层；中央区域承载今日任务、阅读、练习、检索和关联图；右侧栏显示当前上下文并支持提问。面板边界可调整，但中央内容始终拥有最高空间优先级。

窄屏采用渐进收起：先把 AI 家教变为右侧抽屉，再把知识库栏变为左侧抽屉。核心学习任务不依赖横向滚动。

**The Context Follows Selection Rule.** 切换知识库、任务、题目或关联图时，中央标题与右侧助手必须同步更新当前上下文。

## Elevation & Depth

界面以色调差和一像素边界分层。阴影只用于浮层、抽屉和拖动中的面板，不用于静态内容卡片。

**The Flat Workspace Rule.** 常驻面板保持扁平；只有真正离开文档平面的界面才获得阴影。

## Shapes

面板边界接近直角，按钮、输入框和选中行使用轻微圆角。圆角服务于点击目标识别，不把每个信息块包装成独立卡片。

## Do's and Don'ts

### Do:

- **Do** 让知识库名称与其关联图按钮成为两个独立、清晰命名的点击目标。
- **Do** 使用中央标签页打开关联图，保留原学习任务和知识库选择。
- **Do** 让主操作、当前项、键盘焦点同时拥有颜色之外的形态提示。
- **Do** 在 API Key 缺失或关系数据为空时显示诚实、可执行的状态说明。

### Don't:

- **Don't** 把所有功能都连接到最高级导航或在首屏堆叠全部表单。
- **Don't** 重复显示顶部状态、元信息栏和底部状态栏表达同一信息。
- **Don't** 使用无功能的按钮、装饰性状态胶囊或无法解释的图标。
- **Don't** 把班级管理提升到个人学习首页的主要层级。
